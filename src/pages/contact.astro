---
// src/pages/contact.astro
import BaseLayout from '../layouts/BaseLayout.astro';
---
<BaseLayout>
  <section class="contact-section">
    <h2>Contáctanos</h2>
    <p>Estamos aquí para ayudarte. Si tienes preguntas o necesitas asistencia, no dudes en ponerte en contacto con nosotros.</p>
    <a href="mailto:contacto@tubus.cl" class="email-link">Envíanos un Correo</a>
  </section>
</BaseLayout>

<style>
  .contact-section {
    padding: 60px 20px;
    background: url('/assets/hero-bg.webp') no-repeat center center;
    background-size: cover;
    color: #f8f9f9; /* Color de texto para mejor contraste */
    text-align: center;
    position: relative;
    min-height: 100vh; /* Asegura que la sección tenga al menos el alto de la ventana del navegador */
  }

  .contact-section::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.5); /* Fondo oscuro con opacidad */
    z-index: 1;
  }

  h2 {
    font-size: 2.5rem;
    margin-bottom: 20px;
    z-index: 2; /* Asegura que el texto esté por encima del fondo oscuro */
    position: relative;
  }

  p {
    font-size: 1.2rem;
    margin-bottom: 20px;
    z-index: 2; /* Asegura que el párrafo esté por encima del fondo oscuro */
    position: relative;
  }

  .email-link {
    display: inline-block;
    padding: 10px 20px;
    background-color: #e63946;
    color: #f8f9f9;
    text-decoration: none;
    border-radius: 5px;
    transition: background-color 0.3s ease;
    z-index: 2; /* Asegura que el enlace esté por encima del fondo oscuro */
    position: relative;
  }

  .email-link:hover {
    background-color: #d62839;
  }
</style>
